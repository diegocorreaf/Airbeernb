<div class="navbar navbar-expand-sm navbar-light navbar-lewagon">
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item dropdown">
        <%= image_tag "avatar.jpg", class: "avatar dropdown-toggle", id: "navbarDropdown", "data-toggle": "dropdown" if current_user %>
        <div class="dropdown-menu dropdown-menu-left" aria-labelledby="navbarDropdown">
          <a class="dropdown-item" href="#">Profile</a>
          <%= link_to "Sign out", destroy_user_session_path, method: :delete, class: 'dropdown-item' %>
        </div>
      </li>
      <li class="nav-item active">
        <%= link_to 'Add beer', new_product_path, class: 'nav-link' %>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#">Products</a>
      </li>
    </ul>
  </div>

  <div class="airbnbeer">
   <h2 class='navbar-title'>Airbnbeer <i class="fas fa-beer"></i></h2>
  </div>
  
  
  <div class="search-bar">
  <div class="field">
     <%= form_tag products_path, method: :get do %>
    <%= text_field_tag :query, params[:query], class: "form-control",
        placeholder: "Give me a beer"%>
  </div>
    <%= submit_tag "Search", class: "btn btn-outline-light my-2 my-sm-0" %>
  <% end %>
  </div>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

</div>